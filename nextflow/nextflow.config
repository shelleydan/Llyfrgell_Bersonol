process {
	executor = 'slurm'
	containerEngine = 'apptainer'
}

// Enable Wave for Bioconda Containers
enableWave = true
docker.enabled = false

// Enable Singularity

singularity {
	enabled = true
	autoMounts = true
	cacheDir = '/mnt/scratch45/${USER}/singularity/cache'
}
